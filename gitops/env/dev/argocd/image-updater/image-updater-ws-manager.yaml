apiVersion: argocd-image-updater.argoproj.io/v1alpha1
kind: ImageUpdater
metadata:
  name: intrachat-dev-image-updater-ws-manager
  namespace: argocd
spec:
  namespace: argocd
  commonUpdateSettings:
    updateStrategy: "digest"
    pullSecret: "pullsecret:argocd/ghcr-credentials"
  writeBackConfig:
    method: "git"
    gitConfig:
      branch: "main"
      repository: "https://github.com/MichelEloka/Intrachat.git"
      writeBackTarget: "kustomization:./ws-manager/overlay"
  applicationRefs:
    - namePattern: "intrachat-dev"
      images:
        - alias: "wsmanager"
          imageName: "ghcr.io/micheleloka/intrachat-ws-manager:latest"
          manifestTargets:
            kustomize:
              name: "intrachat/ws-manager"
