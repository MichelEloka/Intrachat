apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-service
spec:
  template:
    spec:
      imagePullSecrets:
        - name: ghcr-credentials
      containers:
        - name: auth-service
          env:
            - name: KEYCLOAK_ISSUER_URI
              value: http://172.31.253.146/auth/realms/intrachat
            - name: APP_CORS_ALLOWED_ORIGINS
              value: http://dev.intrachat.local,http://172.31.253.146,http://localhost:5173
